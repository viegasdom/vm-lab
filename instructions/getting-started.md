## VM Lab

This lab runs an Ubuntu 22.04 VM alongside an Alpine container on the same network.

Use the terminal to interact with the container and verify connectivity to the VM.

```bash
ping -c 3 ubuntu.vm.sandbox.internal
```
